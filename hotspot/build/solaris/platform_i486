os_family = solaris

arch = x86

arch_model = x86_32

os_arch = solaris_x86

os_arch_model = solaris_x86_32

lib_arch = i386

compiler = sparcWorks

gnu_dis_arch = i386

sysdefs = -DSOLARIS -DSPARC_WORKS -DIA32
