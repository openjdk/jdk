name = NSS

showInfo = true

slot = 1

library = ${pkcs11test.nss.lib}

disabledMechanisms = {
    CKM_SHA224_HMAC
    CKM_SHA256_HMAC
}

nssArgs = "configdir='${pkcs11test.nss.db}' certPrefix='' keyPrefix=''"
