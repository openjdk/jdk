package jdk.internal.lang.stable;

import java.lang.invoke.StableValue;

// Wrapper interface to allow internal implementations
public non-sealed interface InternalStableValue<T> extends StableValue<T> { }
